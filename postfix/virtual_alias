# Catch-all virtual alias mapping
# Forward all emails to our processing script
# Exclude system addresses (postmaster, abuse, etc.)
/^(?!postmaster|abuse|admin|root|noreply)(.*)@DOMAIN_PLACEHOLDER$/   receive-email
